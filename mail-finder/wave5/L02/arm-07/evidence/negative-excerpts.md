# Negative / gate excerpts — Wave5 L02 Arm 07

## Jonathan Duarte — `https://crossboundary.com/people/jonathan-duarte/`

```html
<h1 class="font-pageTitle mb-sm">Jonathan Duarte</h1>
<div class="body">Partner, Head of LAC Advisory</div>
...
<a class="font-sectionTitle" href="https://www.linkedin.com/in/duartejonathan/" target="_blank" aria-label="Linkedin">
```

No `mailto:` or `@crossboundary.com` on the people bio page.

## Josep Oriol — `https://www.okavango-capital.com/our-team.html`

```html
<h2 ...>Josep Oriol<br />Managing Partner<br />...</h2>
```

Team block names Josep Oriol; no personal mailto on team page. Contact page publishes generic `info@okavango-capital.com` only.

## Josep Oriol — privacy PDF (not FOUND)

PDF link annotation: `mailto:j.oriol@okavango-capital.com` — PDF body does not print *Josep Oriol* alongside that mailbox (initial-only local part; name co-occurrence gate not met).

## Jason Sydow — `https://www.n47.com/team`

Fetched team HTML (~64k) and full sitemap: no `Jason Sydow` / `sydow` slug; `/team/jason-sydow` returns 404. Zero `mailto:` on team page.

## J.P. Keating — `https://www.prochain.vc/`

Wix site pages (`/`, `/blank`, `/values`, `/invest`) fetched; zero `mailto:` and zero `Keating` in page source. Sitemap lists only `/`, `/blank`, `/values`, `/invest` (no team path).

## James Todd — Oikocredit (access)

All crawled first-party paths return Cloudflare managed challenge (HTTP 403, “Just a moment…”) — team/about HTML not available for mailto inspection in this environment.
